{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('css/list.css') }}" rel="stylesheet" />
{% endblock %}

{% block body %}
    <h1>Todolist</h1>

    <div>
        {{ form_start(addNewItemForm) }}
        {{ form_widget(addNewItemForm) }}
        {{ form_end(addNewItemForm) }}
    </div>

    <div>
        <table>
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Due date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for item in listData %}
                <tr>
                    <td>{{ item.description }}</td>
                    <td>{{ item.dueDate | date('d.m.Y') }}</td>
                    <td><a href="{{ path('viewTodoItem', {'itemId': item.id}) }}">Details</a> </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}